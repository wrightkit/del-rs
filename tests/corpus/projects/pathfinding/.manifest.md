# Project fixture: pathfinding

Real-world example project shipped with upstream OSTW: `Examples/Jump-pad pathfinding/` at commit `817c1db4bace52123f054ffe10d3d8a06052e687`.

## Files

| File | Upstream origin | Kind | Expected outcome |
|---|---|---|---|
| `Pathfinding.del` | `Examples/Jump-pad pathfinding/Pathfinding.del` | OSTW source (entry point) | `ok` — compiles upstream; uses classes, macros, subroutines, rule order (`-1`), `foreach`, named arguments, classic formatted strings `<"...<0>..."`, `new Pathmap("Map.pathmap")` |
| `Debug Camera.del` | `Deltinteger/Deltinteger/Modules/Debug Camera.del` (imported by `Pathfinding.del` via `import "!Debug Camera.del";`) | OSTW source (imported) | `ok` — bundled module; uses `playervar` (player-camera), `globalvar`, `if/else`, ternary, `!=`, `!` boolean not |
| `customGameSettings.json` | `Examples/Jump-pad pathfinding/customGameSettings.json` | Lobby settings JSON (imported) | `ok` — imported via `import "customGameSettings.json";`; JSON has no comment syntax so no header block |
| `Map.pathmap` | `Examples/Jump-pad pathfinding/Map.pathmap` | Pathmap data (JSON) | `ok` — loaded at runtime via `new Pathmap("Map.pathmap")`; JSON has no comment syntax so no header block |

## Import resolution notes

- Upstream, the `!` prefix in `import "!Debug Camera.del";` resolves relative to the compiler's
  bundled `Modules/` directory (`Extras.CombinePathWithDotNotation`, `Parse/Import/Importer.cs`).
  In this corpus the dependency file is placed in the same directory as the importer under its
  exact upstream filename; a project loader should be configured with the corpus
  `modules/`-style resolution or the file must be resolvable relative to the importing file.
  See `docs/syntax-notes.md` (imports).
- `Pathfinding.del` imports `customGameSettings.json`; the corpus loader must handle the
  lobby-settings import path (`.json` extension, `Parse/Import/Importer.cs`).
- The `pathmap` type (`globalvar Pathmap pathmap = new Pathmap("Map.pathmap")`) is provided by
  the upstream compiler (`Pathfinder/`), not by the source itself. Parsing is unaffected;
  semantic analysis of `pathmap` requires the compiler-utility surface.

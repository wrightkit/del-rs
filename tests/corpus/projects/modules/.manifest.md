# Project fixture: modules

Bundled OSTW modules shipped in `Deltinteger/Deltinteger/Modules/` at commit `817c1db4bace52123f054ffe10d3d8a06052e687`.

## Files

| File | Upstream origin | Kind | Expected outcome |
|---|---|---|---|
| `PathfindEditor.del` | `Deltinteger/Deltinteger/Modules/PathfindEditor.del` | OSTW source (entry point) | `ok` — pathmap editor; imports `!Container.del`; rule order `-1`; `#` action comments; named args; `CreateHudText(...)`; `RuleSetting` style `rule: "Commands" -1` |
| `Container.del` | `Deltinteger/Deltinteger/Modules/Container.del` | OSTW source (imported by `PathfindEditor.del` via `import "!Container.del";`) | `ok` — macros (`public define ScopeData: ...`), classes, constructors with `ref define` params, `foreach`, `<Type>expr` casts, rule-level `if` conditions |
| `Debug Tools.del` | `Deltinteger/Deltinteger/Modules/Debug Tools.del` | OSTW source | `ok` — `CreateHudText` with named arguments; `<"<0> <<1>, <2>>"` classic formatted strings; ray-cast helpers |
| `Debug Camera.del` | `Deltinteger/Deltinteger/Modules/Debug Camera.del` | OSTW source | `ok` — player-variable camera system; `Event.OngoingPlayer`; ternary; `in` parameters |

## Notes

- File names match the upstream module names exactly so `!`-prefixed imports resolve within the
  corpus directory layout (`PathfindEditor.del` imports `!Container.del`).
